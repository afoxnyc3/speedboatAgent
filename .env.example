# RAG Agent Environment Configuration
# All keys marked as Required are needed for core functionality

# Required: OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Required: Weaviate Vector Database
WEAVIATE_HOST=your_weaviate_cluster_url
WEAVIATE_API_KEY=your_weaviate_api_key

# Required: GitHub Integration
GITHUB_TOKEN=your_github_personal_access_token
GITHUB_WEBHOOK_SECRET=your_webhook_secret

# Required: Memory Service (Mem0)
MEM0_API_KEY=your_mem0_api_key

# Required: Web Crawling (Firecrawl)
FIRECRAWL_API_KEY=your_firecrawl_api_key

# Cache & Queue (Upstash Redis)
UPSTASH_REDIS_URL=your_upstash_redis_url
UPSTASH_REDIS_TOKEN=your_upstash_redis_token

# Optional: Rate Limiting Configuration (with environment-based tuning)
# Global rate limiting settings (defaults: 100 req/min, 60s window)
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=60000

# Endpoint-specific rate limiting (overrides for specific endpoints)
RATE_LIMIT_CHAT_MAX=20          # Chat endpoints (resource intensive)
RATE_LIMIT_SEARCH_MAX=50        # Search endpoints (medium usage)
RATE_LIMIT_CACHE_MAX=200        # Cache endpoints (lightweight)
RATE_LIMIT_MEMORY_MAX=100       # Memory endpoints (lightweight)

# Trusted IPs bypass (comma-separated list of IPs that bypass rate limits)
# Note: Internal IPs (10.x.x.x, 192.168.x.x, 172.16.x.x, 127.x.x.x) auto-bypass
RATE_LIMIT_TRUSTED_IPS=

# Monitoring & Error Tracking
SENTRY_DSN=your_sentry_dsn_url
VERCEL_ENV=development

# Application Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: Analytics (uncomment if needed)
# NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_POSTHOG_KEY=your_posthog_key
# NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# Optional: Database (uncomment if needed)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Optional: Authentication (uncomment if needed)
# NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=your_nextauth_secret_here

# Optional: Feature Flags
# ENABLE_EXPERIMENTAL_FEATURES=false